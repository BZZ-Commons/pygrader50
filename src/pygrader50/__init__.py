"""pygrader50 — the BZZ autograder engine for Classroom 50.

`python -m pygrader50` grades a student checkout with pytest and pylint and
writes the `classroom50/result/v1` payload the runner expects.
"""

__version__ = '2.0.0'
